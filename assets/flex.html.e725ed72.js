import{_ as a,a as o}from"./02-10-24.79854212.js";import{_ as c,r as l,o as i,c as t,b as e,d as n,e as p,a as d}from"./app.8a7fda1c.js";const r="/flex_learn_manual/image/02-10-20.png",u="/flex_learn_manual/image/02-10-22.png",m="/flex_learn_manual/image/02-10-23.png",x="/flex_learn_manual/image/02-10-25.png",f="/flex_learn_manual/image/02-10-26.png",v="/flex_learn_manual/image/02-10-27.png",k="/flex_learn_manual/image/02-10-35.png",g="/flex_learn_manual/image/02-10-36.png",b="/flex_learn_manual/image/02-10-37.png",_="/flex_learn_manual/image/02-10-28.png",h="/flex_learn_manual/image/02-10-29.png",y="/flex_learn_manual/image/02-10-30.png",w="/flex_learn_manual/image/02-10-31.png",z="/flex_learn_manual/image/02-10-32.png",S="/flex_learn_manual/image/02-10-33.png",V="/flex_learn_manual/image/02-10-34.png",q={},N=d(`<h1 id="📕-flex-元素-flex" tabindex="-1"><a class="header-anchor" href="#📕-flex-元素-flex" aria-hidden="true">#</a> 📕 flex 元素：flex</h1><p>在 flex 元素中使用频率较高的应该是 <code>flex</code> 这个属性了，该属性是一个简写属性，包含了 <code>flex-grow</code>、<code>flex-shrink</code> 以及 <code>flex-basis</code> 这三个属性。由于这三个属性的属性值自身特点，所以，<code>flex</code> 属性的单位值可以为：</p><ul><li>无单位、纯数字，且必须大于等于 <strong>0</strong>，主要作用于 <code>flex-grow</code> 和 <code>flex-shrink</code>；</li><li>具有有效单位的值，主要作用于 <code>flex-basis</code>；</li><li>特定的关键词；</li></ul><p>而作为简写方式存在的 <code>flex</code> 属性，所拥有的属性值包含了：一个值、两个值以及三个值的写法。在进一步了解这三种属性值写法之前，我们要先记住上面提到的 <code>flex</code> 属性值的写法特色。</p><blockquote><p><strong>注意：</strong> <code>flex</code> 属性中每个属性其自身原有的默认属性值保持不变，如在 <code>flex</code> 未特别处理声明，将会使用默认值。</p></blockquote><h3 id="一个值的-flex-属性" tabindex="-1"><a class="header-anchor" href="#一个值的-flex-属性" aria-hidden="true">#</a> 一个值的 <code>flex</code> 属性</h3><p>在日常前端开发过程中，普遍前端开发人员在写 flex 布局时，是直接使用 <code>flex: 1;</code> 或者 <code>flex: 0;</code> 来使用的。对于这样的处理方式，相对来说 <code>flex</code> 这个简写方式带来了不少便利，并且能够快速满足 flex 布局中每个元素的伸缩特性。</p><p>但这样盲目地使用，相对而言是不够严谨的。在本文开头已经提到了 <code>flex</code> 的几种属性值的写法，根据这几种写法，大概罗列后为：</p><div class="language-css line-numbers-mode" data-ext="css"><pre class="language-css"><code><span class="token comment">/* 
  file: flex_0048.css
  flex 仅有一个属性值时的情况，主要是为了观察在 DevTools 中解析 flex 后每个值的变化；
*/</span>
<span class="token selector">.demo</span> <span class="token punctuation">{</span>
  <span class="token property">flex-wrap</span><span class="token punctuation">:</span> wrap<span class="token punctuation">;</span>
  <span class="token comment">/* 在这里给 flex 容器设置了高度 */</span>
  <span class="token property">border</span><span class="token punctuation">:</span> 1px solid #000<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token comment">/* 仅为了观察 flex 属性值的变化，隐藏部分不需要的元素 */</span>
<span class="token selector">.item:nth-child(n+9)</span> <span class="token punctuation">{</span>
  <span class="token property">display</span><span class="token punctuation">:</span> none<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token selector">.item_1</span> <span class="token punctuation">{</span>
  <span class="token property">flex</span><span class="token punctuation">:</span> 3<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token selector">.item_2</span> <span class="token punctuation">{</span>
  <span class="token property">flex</span><span class="token punctuation">:</span> 1<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token selector">.item_3</span> <span class="token punctuation">{</span>
  <span class="token property">flex</span><span class="token punctuation">:</span> 0<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token selector">.item_4</span> <span class="token punctuation">{</span>
  <span class="token property">flex</span><span class="token punctuation">:</span> 0%<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token selector">.item_5</span> <span class="token punctuation">{</span>
  <span class="token property">flex</span><span class="token punctuation">:</span> 200px<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token selector">.item_6</span> <span class="token punctuation">{</span>
  <span class="token property">flex</span><span class="token punctuation">:</span> auto<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token selector">.item_7</span> <span class="token punctuation">{</span>
  <span class="token property">flex</span><span class="token punctuation">:</span> none<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token selector">.item_8</span> <span class="token punctuation">{</span>
  <span class="token property">flex</span><span class="token punctuation">:</span> initial<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>一步到位，<a href="../../demo.html?id=48">把几种情况展示一下</a>。以上 <a href="../../demo.html?id=48">demo</a> 对于浏览器的渲染结果暂不做考虑，而仅从 DevTools 中去查看每个 <strong>item</strong> 在不同的 <code>flex</code> 属性值下的解析结果。</p><h4 id="item-1-item-2" tabindex="-1"><a class="header-anchor" href="#item-1-item-2" aria-hidden="true">#</a> item_1 &amp;&amp; item_2</h4><p>显然，这两个 <strong>item</strong> 的主要区别就是数字的大小不同，主要是考虑更容易区分 <code>flex-grow</code> 和 <code>flex-shrink</code> 的默认值。</p><p>首先我们来看 <code>flex: 3;</code> 的结果：</p><img src="`+r+'" style="zoom:50%;"><p>从结果可以看到，<code>flex-grow</code> 的值改变了，而 <code>flex-shrink</code> 保持着默认值为 <code>1</code> 的结果，至于 <code>flex-basis</code> 不再是 <code>auto</code> 这个默认值，而是转为 <code>0%</code>，同理，<code>flex: 1;</code> 的结果，应该也就是 <code>flex-grow</code> 为 <code>1</code>，具体的我们可以看 <strong>.item_2</strong> 的结果。</p><img src="'+a+'" style="zoom:50%;"><p>所以，最终，<code>flex</code> 值为大于 0 的无单位值的数字时，将代表该元素是可伸缩的元素，至于伸展的比例就需要根据最终的值而定。</p><h4 id="item-3" tabindex="-1"><a class="header-anchor" href="#item-3" aria-hidden="true">#</a> item_3</h4><p>平时我们需要让一个 flex 元素可伸缩的，都会写 <code>flex: 1;</code>，反之，如果不想要一个 flex 元素可伸缩的话，那么就是写 <code>flex: 0;</code>。而这个 <strong>0</strong> 所代表的都有哪些呢？是将三个属性值都设置为 <strong>0</strong> 吗？其实并不是，而只是把 <code>flex-grow</code> 设置为 <strong>0</strong> 而已。</p><img src="'+u+'" style="zoom:50%;"><h5 id="临时小结一下" tabindex="-1"><a class="header-anchor" href="#临时小结一下" aria-hidden="true">#</a> 临时小结一下</h5><p>从上面可以结果我们可以发现，当 <code>flex</code> 的属性值为一个值的时候，且该值不具备单位值时，相当于展开后，只是修改了 <code>flex-grow</code> 的值，而不会改变 <code>flex-shrink</code> 的值。至于 <code>flex-basis</code> 则从默认值 <code>auto</code> 转为 <code>0%</code>，从页面效果来说，并没太大差别。</p><p>简单理解就是：<code>flex</code> 的属性值没有单位值时，相当于在修改 <code>flex-grow</code> 的值。</p><h4 id="item-4" tabindex="-1"><a class="header-anchor" href="#item-4" aria-hidden="true">#</a> item_4</h4><p>在这个 flex 元素中，我们给的单位值是 <code>0%</code>，最终得到的结果是：</p><img src="'+m+'" style="zoom:50%;"><p>从结果来看，<code>flex: 0%;</code> 与 <code>flex: 1;</code> 的结果是相同的。</p><h4 id="item-5" tabindex="-1"><a class="header-anchor" href="#item-5" aria-hidden="true">#</a> item_5</h4><p>那么我们把 <code>0%</code> 改为 <code>200px</code> 的话，得到的结果会是怎么样呢？</p><img src="'+o+'" style="zoom:50%;"><h5 id="临时小结一下-1" tabindex="-1"><a class="header-anchor" href="#临时小结一下-1" aria-hidden="true">#</a> 临时小结一下</h5><p>从这两个 <strong>item</strong> 的结果来看，当 <code>flex</code> 的值具有单位值时，会将 <code>flex-grow</code> 的默认值从 <code>0</code> 转为 <code>1</code>，让该元素变成可伸展；而不会改变 <code>flex-shrink</code> 的默认值，因此该元素也是可缩的；而最终有单位值的这个属性值是作用于 <code>flex-basis</code>。</p><p>简单理解就是：<code>flex</code> 的属性值如有单位值将改变该元素的初始大小，并且该元素是可伸缩的。</p><h4 id="item-6" tabindex="-1"><a class="header-anchor" href="#item-6" aria-hidden="true">#</a> item_6</h4><p><code>flex: auto;</code> 看到这里的 <code>auto</code>，回想一下，这其实是 <code>flex-basis</code> 的默认值。仅作用于 <code>flex-basis</code> 时，其他两个属性值在未指定的情况下，回头看一下 <strong>item_5</strong> 的结果，所以，现在我们得到的最终结果就是：</p><img src="'+x+'" style="zoom:50%;"><p>一个可伸缩，并且宽高是由内容决定的 flex 元素。从效果上来看与 <code>flex: 1;</code> 并没有太大的差异。</p><h4 id="item-7" tabindex="-1"><a class="header-anchor" href="#item-7" aria-hidden="true">#</a> item_7</h4><p><code>none</code> 在 CSS 中绝大部分情况下所代表的是类似<strong>无</strong>、<strong>没有</strong>之类的含义，所以 <code>flex: none;</code> 也不例外，表示当前 flex 元素不可伸缩，换言之，就是不具备弹性的功能了。仅只能依靠元素自身的内容来撑开宽高，因为 <code>flex: none;</code> 解析后的情况是：</p><img src="'+f+'" style="zoom:50%;"><p>需要注意，<code>flex: 0;</code> 并不等于 <code>flex: none;</code> 如果想要让一个 flex 不具备伸缩弹性功能，是要写 <code>flex: none;</code> 而不是 <code>flex: 0;</code>。</p><h4 id="item-8" tabindex="-1"><a class="header-anchor" href="#item-8" aria-hidden="true">#</a> item_8</h4><p><code>initial</code> 在 CSS 中所代表的是初始值，所以 <code>flex: initial;</code> 这个就好理解了，解析后是：</p><img src="'+v+'" style="zoom:50%;"><p>再回想一下这三个属性的默认值是什么，那么就可以知道这里的每个 <code>initial</code> 所代表的含义了，也就是说 <code>flex: initial;</code> 等同于 <code>flex: 0 1 auto;</code>，也就是 <code>flex-grow: 0; flex-shrink: 1; flex-basis: auto;</code> 当我们需要对某个 flex 元素进行重置为初始值的时候，就可以考虑使用了。</p><h4 id="小结" tabindex="-1"><a class="header-anchor" href="#小结" aria-hidden="true">#</a> 小结</h4><p>从以上的几种情况来看，<code>flex</code> 属性值为单个属性值时，是否带有单位将会影响最终所作用的属性值，但无论如何，单属性值是不会修改 <code>flex-shrink</code> 的值，而是：</p><ul><li>无单位值：虽然仅作用于 <code>flex-grow</code> ，但是会将 <code>flex-basis</code> 的默认值 <code>auto</code> 转为 <code>0%</code>；</li><li>有单位值：再改变 <code>flex-grow</code> 为 <code>1</code> 的情况下，将具体的值作用于 <code>flex-basis</code>；</li><li>关键词：不同的关键词代表着不同的含义 <ul><li><code>auto</code>：可伸缩，初始大小为元素自身内容所撑开的宽高；</li><li><code>none</code>：不可伸缩，元素仅依靠自身内容撑开；</li><li><code>initial</code>：设置元素弹性特性为默认值，可缩不可伸，初始大小为元素自身内容所撑开的宽高；</li></ul></li></ul><h3 id="两个值的-flex-属性" tabindex="-1"><a class="header-anchor" href="#两个值的-flex-属性" aria-hidden="true">#</a> 两个值的 <code>flex</code> 属性</h3><p>当 <code>flex</code> 属性有两个属性值的时候，虽然不区分前后顺序，但是需要注意有且只能有一个属性值是有单位值，或者关键词。以下三种情况会被情况会被浏览器视为无效属性值：</p><ul><li>含有 <strong>initial</strong> 属性值时，如：<code>flex: initial 200px;</code><img src="'+k+'" style="zoom:50%;"></li><li>含有 <strong>none</strong> 属性值时，如：<code>flex: 7 none;</code><img src="'+g+'" style="zoom:50%;"></li><li>两个属性值都有单位值时，如：<code>flex: 200px 0%;</code><img src="'+b+`" style="zoom:50%;"></li></ul><div class="language-css line-numbers-mode" data-ext="css"><pre class="language-css"><code><span class="token comment">/* 
  file: flex_0049.css
  flex 有两个属性值时的情况，主要是为了观察在 DevTools 中解析 flex 后每个值的变化；
*/</span>
<span class="token selector">.demo</span> <span class="token punctuation">{</span>
  <span class="token property">flex-wrap</span><span class="token punctuation">:</span> wrap<span class="token punctuation">;</span>
  <span class="token comment">/* 在这里给 flex 容器设置了高度 */</span>
  <span class="token property">border</span><span class="token punctuation">:</span> 1px solid #000<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token selector">.item_1</span> <span class="token punctuation">{</span>
  <span class="token property">flex</span><span class="token punctuation">:</span> 3 200px<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token selector">.item_2</span> <span class="token punctuation">{</span>
  <span class="token property">flex</span><span class="token punctuation">:</span> 200px 2<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token selector">.item_3</span> <span class="token punctuation">{</span>
  <span class="token property">flex</span><span class="token punctuation">:</span> 0 4<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token selector">.item_4</span> <span class="token punctuation">{</span>
  <span class="token property">flex</span><span class="token punctuation">:</span> 4 6<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token selector">.item_5</span> <span class="token punctuation">{</span>
  <span class="token property">flex</span><span class="token punctuation">:</span> 5 auto<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token selector">.item_6</span> <span class="token punctuation">{</span>
  <span class="token property">flex</span><span class="token punctuation">:</span> auto 6<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token comment">/* 以下情况会被浏览器视为无效属性值 */</span>
<span class="token selector">.item_7</span> <span class="token punctuation">{</span>
  <span class="token comment">/* 如其中一个为 auto，另外一个必定是无单位的值，否则将会报错 */</span>
  <span class="token property">flex</span><span class="token punctuation">:</span> 200px auto<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token selector">.item_8</span> <span class="token punctuation">{</span>
  <span class="token comment">/* 当含有 initial 的时候，也不可以其他属性值存在 */</span>
  <span class="token property">flex</span><span class="token punctuation">:</span> initial 200px<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token selector">.item_9</span> <span class="token punctuation">{</span>
  <span class="token comment">/* 当含有 none 的时候，也不可以其他属性值存在 */</span>
  <span class="token property">flex</span><span class="token punctuation">:</span> 7 none<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token selector">.item_0</span> <span class="token punctuation">{</span>
  <span class="token comment">/* 当出现有两个具有单位值的属性值时 */</span>
  <span class="token property">flex</span><span class="token punctuation">:</span> 200px 0%<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>如上述的 <a href="../../demo.html?id=49">demo</a> 中，<strong>item_1</strong> 到 <strong>item_5</strong> 都是可被正常解析的属性值，从中我们可以看到有一个单位值的或者存在 <code>auto</code> 的都是可以被正常解析，因为是作用于 <code>flex-basis</code> 属性的。那么没有单位值的数字是作用于 <code>flex-grow</code> 呢还是 <code>flex-shrink</code> 呢？</p><p>结合单属性值的情况，我们可以猜想的结果是：</p><ul><li><p><strong>一个有单位值，一个没有单位值的情况</strong>：有单位值的必定是 <code>flex-basis</code>，而无单位值的则会是 <code>flex-grow</code> 的结果，并且这两个值的顺序如何并不会影响最终的结果；</p><p><img src="`+_+'" style="zoom:50%;"><img src="'+h+'" style="zoom:50%;"></p></li><li><p><strong>两个都为无单位的值</strong>：当两个都为无单位的值时，将会根据数值的出现顺序先后作用于 <code>flex-grow</code> 以及 <code>flex-shrink</code>，同时将 <code>flex-basis</code> 调整为 <code>0%</code>； <img src="'+y+'" style="zoom:50%;"><img src="'+w+'" style="zoom:50%;"></p></li><li><p>其中<strong>一个值为 <code>auto</code> 的话</strong>，<strong>另外一个必然是无单位</strong>的属性值，否则将会报错； <img src="'+z+'" style="zoom:50%;"><img src="'+S+'" style="zoom:50%;"><img src="'+V+'" style="zoom:50%;"></p></li></ul><h3 id="三个值的-flex-属性" tabindex="-1"><a class="header-anchor" href="#三个值的-flex-属性" aria-hidden="true">#</a> 三个值的 flex 属性</h3><p>三个值的情况就简单的，展开后分别就是对应 <code>flex-grow</code> 、<code>flex-shrink</code> 已经 <code>flex-basis</code> 这三个属性，书写的时候，对于 <code>flex</code> 属性值的顺序需要注意一下几点：</p><ul><li><code>flex-grow</code> 和 <code>flex-shrink</code> 这<strong>两个无单位的属性值必须写在一起</strong>，且先后顺序必定是先 <code>flex-grow</code>，再是 <code>flex-shrink</code>；</li><li><code>flex-basis</code> 可以在最后一个也可以在第一个位置，唯独<strong>不能在中间</strong>；</li><li>关键词属性值的使用，<strong>仅只有 <code>auto</code> 可以用</strong>，且不能在两个无单位的属性值中间，以及<strong>不能与有单位的属性值同时出现</strong>；</li></ul><h3 id="归纳一下" tabindex="-1"><a class="header-anchor" href="#归纳一下" aria-hidden="true">#</a> 归纳一下</h3><p><code>flex</code> 属性是平时大家使用率较高的一个属性，往往都只是写了 <code>flex: 1;</code>，那么对于单个属性值的情况，我们需要注意有单位值和无单位值的一个区别。接着呢，就是需要注意一下三个属性值的时候，每个值不同所带来的影响，比如：</p><ul><li><code>flex: 1 0 100%</code>;</li><li><code>flex: 0 0 100%;</code></li><li><code>flex: 1 1 50%;</code></li><li>……</li></ul>',61),T={href:"https://mp.weixin.qq.com/s/OsVzMZtJeWpz-8gcQI_ySQ",target:"_blank",rel:"noopener noreferrer"};function B(C,D){const s=l("ExternalLinkIcon");return i(),t("div",null,[N,e("p",null,[n("诸如此类的组合方式最终在一个 flex 布局中所带来的作用和影响会是怎么样的。有兴趣的可以阅读一下我的这篇 2017-12-06 写的公众号文章：《"),e("a",T,[n("又一次想说 flex 布局挖坑给你，信吗？"),p(s)]),n("》")])])}const L=c(q,[["render",B],["__file","flex.html.vue"]]);export{L as default};
